{
	"name": "@edgebus/messenger-bridge",
	"version": "0.1.0",
	"description": "APPROVE ME is a helper service to collect approvement votes via messenger networks like Telegram, Slack, etc...",
	"type": "module",
	"main": "./lib/index.js",
	"types": "./lib/index.d.ts",
	"author": {
		"name": "ZXTeam",
		"email": "devel@edgebus.io",
		"url": "https://edgebus.io/messenger-bridge"
	},
	"contributors": [
		{
			"name": "Maxim Anurin",
			"email": "theanurin@gmail.com",
			"url": "http://www.anurin.name"
		}
	],
	"license": "MIT",
	"devDependencies": {
		"@tsconfig/node22": "^22.0.0",
		"@tsconfig/strictest": "^2.0.5",
		"@types/chai": "^5.2.0",
		"@types/cors": "^2.8.17",
		"@types/lodash": "^4.17.16",
		"@types/mocha": "^10.0.10",
		"@types/mustache": "^4.2.5",
		"@types/uuid": "^10.0.0",
		"chai": "^5.2.0",
		"cross-env": "^7.0.3",
		"dotenv": "^16.4.7",
		"mocha": "^11.1.0",
		"mocha-junit-reporter": "^2.2.1",
		"nyc": "^17.1.0",
		"source-map-support": "^0.5.21",
		"typescript": "^5.8.2"
	},
	"dependencies": {
		"@freemework/common": "^0.14.11",
		"@freemework/hosting": "^0.14.11",
		"@iarna/toml": "^2.2.5",
		"cors": "^2.8.5",
		"express": "^4.21.2",
		"express-prom-bundle": "^8.0.0",
		"lodash": "^4.17.21",
		"mustache": "^4.2.0",
		"prom-client": "^15.1.3",
		"uuid": "^11.1.0"
	},
	"engines": {
		"node": ">=18.0.0 <23"
	},
	"keywords": [],
	"scripts": {
		"clean": "           rm -f tsconfig.tsbuildinfo; find lib \\( -name '*.js' -o -name '*.d.ts' -o -name '*.map' \\) -exec rm {} \\; ; find test \\( -name '*.js' -o -name '*.d.ts' -o -name '*.map' \\) -exec rm {} \\;",
		"build": "           tsc",
		"build:dist": "      rm -rf .dist && tsc --project tsconfig-dist.json && cp README.md .dist/ && cp -a bin .dist/ && cp -a res .dist/ && jq '{ name: .name, version: .version, description: .description, author: .author, contributors: .contributors, license: .license, keywords: .keywords, repository: .repository, bugs: .bugs, engines: .engines, homepage: .homepage, main: \"lib/index.js\", types: \"lib/index.d.ts\", dependencies: .dependencies }' package.json > .dist/package.json",
		"test": "            tsc && nyc --exclude submodules --exclude test --extension=\".ts\" --reporter=lcov --reporter=text --reporter=text-summary mocha --require source-map-support/register --recursive --timeout 10000 \"test/**/*.test.js\"",
		"test:only": "       tsc && mocha --require source-map-support/register \"test/**/*.test.js\" --recursive --timeout 10000",
		"start": "           cross-env NODE_OPTIONS=--trace-warnings node --require dotenv/config --require source-map-support/register bin/app.js --config-toml-file=messenger-bridge.toml --config-toml-file=messenger-bridge-devel.toml --config-env"
	},
	"private": true
}
